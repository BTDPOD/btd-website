---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="Guests — Behind the Door Podcast"
  description="The guests of Behind the Door — home service business owners, world-class technicians, and people who built something real in the trades."
>
  <!-- Hero -->
  <section class="guests-hero noise-overlay">
    <div class="container">
      <span class="eyebrow">The Guests</span>
      <h1 class="page-title chrome-text">Real People.<br />Real Stories.</h1>
      <p class="page-sub">
        Every episode, Kaleb sits down with someone who's in it — home service business owners,
        master technicians, and people who built something nobody can ignore.
      </p>
    </div>
  </section>

  <!-- Coming soon state -->
  <section class="guests-section">
    <div class="container">
      <div class="coming-soon">
        <div class="coming-soon-icon">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" width="120" height="120">
            <defs>
              <linearGradient id="gsChrome" x1="0%" y1="0%" x2="100%" y2="100%">
                <stop offset="0%" stop-color="#c0c0c0"/>
                <stop offset="50%" stop-color="#e8e8e8"/>
                <stop offset="100%" stop-color="#909090"/>
              </linearGradient>
            </defs>
            <circle cx="50" cy="50" r="48" fill="url(#gsChrome)"/>
            <circle cx="50" cy="50" r="44" fill="#111"/>
            <text x="50" y="44" text-anchor="middle" dominant-baseline="middle"
                  font-family="Arial Black" font-weight="900" font-size="22"
                  fill="url(#gsChrome)" letter-spacing="1">BTD</text>
            <g stroke="url(#gsChrome)" stroke-width="1.5" opacity="0.7">
              <line x1="22" y1="62" x2="78" y2="62"/>
              <line x1="22" y1="67" x2="78" y2="67"/>
              <line x1="22" y1="72" x2="78" y2="72"/>
              <line x1="37" y1="62" x2="37" y2="72"/>
              <line x1="50" y1="62" x2="50" y2="72"/>
              <line x1="63" y1="62" x2="63" y2="72"/>
            </g>
          </svg>
        </div>
        <h2 class="coming-title chrome-text">Guests Coming Soon</h2>
        <p class="coming-sub">
          The first wave of guests is being confirmed. These are the people with real stories
          from the field — the kind you don't hear in a boardroom. Check back when episodes start dropping.
        </p>
        <a href="/subscribe" class="btn-chrome">Subscribe to Be First</a>
      </div>
    </div>
  </section>

  <!-- What to expect -->
  <section class="expect-section">
    <div class="container">
      <h2 class="expect-title chrome-text">Who We Bring On</h2>
      <p class="expect-sub">
        Behind the Door isn't interested in polished PR narratives. We bring on the people doing
        the actual work — and the people who've been through enough that they have something
        real to say.
      </p>
      <div class="expect-grid">
        <div class="expect-card">
          <div class="card-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/>
            </svg>
          </div>
          <h3>Master Technicians</h3>
          <p>The HVAC tech who can diagnose anything. The plumber who's seen it all. The electrician who built a team from scratch. Their knowledge deserves an audience.</p>
        </div>
        <div class="expect-card">
          <div class="card-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="2" y="7" width="20" height="14" rx="2" ry="2"/>
              <path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16"/>
            </svg>
          </div>
          <h3>Business Owners</h3>
          <p>People who took the leap — started their own home service company, grew it through chaos, and learned lessons nobody teaches in business school.</p>
        </div>
        <div class="expect-card">
          <div class="card-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
            </svg>
          </div>
          <h3>Industry Leaders</h3>
          <p>Vendors, coaches, recruiters, and innovators who are actively shaping the future of home services — with no agenda except to help the trades grow.</p>
        </div>
        <div class="expect-card">
          <div class="card-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"/>
              <path d="M12 8v4l3 3"/>
            </svg>
          </div>
          <h3>The Comeback Stories</h3>
          <p>People who hit rock bottom in their career or business, rebuilt, and came out the other side stronger. These are the stories that actually help people.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Guest CTA -->
  <section class="guest-cta-section noise-overlay">
    <div class="container">
      <div class="cta-inner">
        <div class="cta-text">
          <h2 class="cta-title chrome-text">Want to Be a Guest?</h2>
          <p>
            If you've got a story worth telling — whether you're a technician, business owner,
            or someone who's been in the trenches of the trades — we want to hear from you.
            No blue-chip resume required. Just a real story and the guts to tell it.
          </p>
        </div>
        <a href="/contact" class="btn-chrome-lg">Apply to Be a Guest</a>
      </div>
    </div>
  </section>
</Layout>

<style>
  .container { max-width: 1100px; margin: 0 auto; }

  .guests-hero {
    background: linear-gradient(160deg, #16181a, #1e2022);
    padding: 5rem 1.5rem 4rem;
    border-bottom: 1px solid #2e3032;
  }

  .eyebrow {
    display: block;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: #6a6e72;
    margin-bottom: 0.75rem;
  }

  .page-title {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 900;
    line-height: 1.1;
    margin: 0 0 1rem;
  }

  .chrome-text {
    background: linear-gradient(135deg, #c0c0c0 0%, #ffffff 20%, #a8a8a8 40%, #e8e8e8 60%, #909090 80%, #d4d4d4 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .page-sub {
    color: #6a6e72;
    font-size: 1rem;
    max-width: 560px;
    line-height: 1.65;
    margin: 0;
  }

  /* Coming soon */
  .guests-section {
    padding: 6rem 1.5rem;
  }

  .coming-soon {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 1.5rem;
    max-width: 500px;
    margin: 0 auto;
  }

  .coming-soon-icon { opacity: 0.5; }

  .coming-title {
    font-size: clamp(1.5rem, 4vw, 2.5rem);
    font-weight: 900;
    margin: 0;
  }

  .coming-sub {
    color: #6a6e72;
    line-height: 1.65;
    font-size: 0.95rem;
    margin: 0;
  }

  /* What to expect */
  .expect-section {
    padding: 5rem 1.5rem;
    background: #16181a;
    border-top: 1px solid #2e3032;
    border-bottom: 1px solid #2e3032;
  }

  .expect-title {
    font-size: clamp(1.5rem, 3.5vw, 2.25rem);
    font-weight: 900;
    margin: 0 0 0.75rem;
    text-align: center;
  }

  .expect-sub {
    color: #6a6e72;
    text-align: center;
    max-width: 560px;
    margin: 0 auto 3rem;
    line-height: 1.65;
    font-size: 0.95rem;
  }

  .expect-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }

  .expect-card {
    background: #1e2022;
    border: 1px solid #2e3032;
    border-radius: 12px;
    padding: 2rem;
    display: flex;
    flex-direction: column;
    gap: 0.875rem;
    transition: border-color 0.25s;
  }

  .expect-card:hover { border-color: #4a4e52; }

  .card-icon {
    width: 56px;
    height: 56px;
    background: rgba(192,192,192,0.07);
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #8a8e92;
  }

  .expect-card h3 {
    font-size: 1.05rem;
    font-weight: 800;
    color: #d4d4d4;
  }

  .expect-card p {
    color: #6a6e72;
    font-size: 0.875rem;
    line-height: 1.65;
    margin: 0;
  }

  /* CTA */
  .guest-cta-section {
    background: linear-gradient(160deg, #1e2022, #16181a);
    padding: 5rem 1.5rem;
  }

  .cta-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 3rem;
    flex-wrap: wrap;
  }

  .cta-text { flex: 1; min-width: 280px; }

  .cta-title {
    font-size: clamp(1.5rem, 3.5vw, 2.25rem);
    font-weight: 900;
    margin: 0 0 1rem;
  }

  .cta-text p {
    color: #6a6e72;
    font-size: 0.95rem;
    line-height: 1.7;
    margin: 0;
    max-width: 520px;
  }

  .btn-chrome {
    display: inline-block;
    padding: 0.875rem 2rem;
    background: linear-gradient(135deg, #c0c0c0 0%, #e8e8e8 25%, #a8a8a8 50%, #d4d4d4 75%, #b8b8b8 100%);
    color: #1a1c1e;
    font-weight: 800;
    font-size: 0.9rem;
    border-radius: 8px;
    text-decoration: none;
    transition: opacity 0.2s, transform 0.15s;
  }

  .btn-chrome:hover { opacity: 0.9; transform: translateY(-1px); }

  .btn-chrome-lg {
    display: inline-block;
    padding: 1rem 2.5rem;
    background: linear-gradient(135deg, #c0c0c0 0%, #e8e8e8 25%, #a8a8a8 50%, #d4d4d4 75%, #b8b8b8 100%);
    color: #1a1c1e;
    font-weight: 800;
    font-size: 1rem;
    border-radius: 8px;
    text-decoration: none;
    white-space: nowrap;
    transition: opacity 0.2s, transform 0.15s;
  }

  .btn-chrome-lg:hover { opacity: 0.9; transform: translateY(-1px); }

  @media (max-width: 768px) {
    .expect-grid { grid-template-columns: 1fr; }
    .cta-inner { flex-direction: column; text-align: center; }
    .cta-text p { margin: 0 auto; }
  }
</style>
